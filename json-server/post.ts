// // import { getJsonObj, dateNow } from "./util.ts";

// export function postRoute(url: string, payload: any) {
//   const path = "";
//   let result = {
//     status: 200,
//   } as {
//     status: number,
//     body?: BodyInit | null,
//   };

//   switch (url) {
//     case "/api/authenticate":
//       // const data = Deno.readFileSync(`.${url}-POST.json`);
//       // //Deno.writeTextFileSync(path, JSON.stringify(summary));
//       // result.status = 200;
//       // result.body = data
//       break;

//     default:
//       // console.log(url, payload);
//       break;
//   }

//   return result;
// }